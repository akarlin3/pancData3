function mdl = fitglme(tbl, formula, varargin)
    % Mock for fitglme
    mdl = struct();
    mdl.Coefficients = struct();
    mdl.Coefficients.Estimate = [0; 0];
    mdl.Coefficients.pValue = [1; 1];
    mdl.Formula = formula;
    % Basic stub
end
